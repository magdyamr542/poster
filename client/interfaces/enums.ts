export enum Server_Routes {
  SIGN_UP = "http://localhost:4000/auth/login",
  GET_ALL_USERS = "http://localhost:4000/user/getUsers",
}

export enum MsgInfoColors {
  SUCCESS = "green",
  FAILURE = "red",
}

export enum pageRoutes {
  SIGN_UP_PAGE = "/register",
  SIGN_IN_PAGE = "/login",
}
